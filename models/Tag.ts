/**
 * @file Declares Tag data type
 */

/**
 * @typedef Tag Represents data type of tag of tuit
 * @property {string} tag the tag of tuit
 */
export default class Tag{
    private tag: string = '';
}